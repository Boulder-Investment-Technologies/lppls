[tool.ruff]
target-version = "py310"
line-length = 88
exclude = ["notebooks/"]

[tool.ruff.lint]
# E741: ambiguous variable name (O is used for Oscillations, a domain term)
ignore = ["E741"]

[tool.ruff.format]
docstring-code-format = true
